# STOATCH


## Use:
  `#stoatch <command> <option>`


## Commands:

  `config`
  
  `task <name>`
  
  `user <name>`
  
  
  `remote [<user>[@<host>[:<port>]]]`
  
  `commit`
  
  `sync`
  
  `server`
  
  
  `database <{none/text/mysql/mongo}=none> <name> [<user>[@<host>[:<port>]]]`
  
  
## Options:

  `--schedule`
  
